/**
 * @import HollowsRPGTokenDocument from "../../documents/token.mjs";
 * @import { Point } from "@common/_types.mjs";
 */

/**
 * A Placeable Object subclass adding system-specific behavior and registered in CONFIG.Token.objectClass.
 */
export default class HollowsRPGToken extends foundry.canvas.placeables.Token {
}